<form  style="float: right;right: 10px;margin-top: -35px;position: absolute;" method="post" action="{{ path('app_projet_delete', {'id': projet.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer le projet de financement');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ projet.id) }}">
    <button class="btn bg-purple btn-flat">Delete</button>
</form>
